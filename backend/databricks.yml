bundle:
  name: iona_cx_bundle

resources:
  apps:
    iona_cx:
      name: iona-cx
      source_code_path: .
      description: IONA CX - Customer Success Dashboard
      resources:
        - name: sql-warehouse
          sql_warehouse:
            id: e0f7c35bbfc5d9cd
            permission: CAN_USE

targets:
  prod:
    mode: production
    default: true
    workspace:
      host: https://dbc-97a2feb3-3e52.cloud.databricks.com
      root_path: /Workspace/Users/arjun.mishra@ifs.com/.bundle/${bundle.name}/${bundle.target}
